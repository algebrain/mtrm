[package]
name = "mtrm-session"
version = "0.3.2"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
mtrm-core = { path = "../core" }
mtrm-layout = { path = "../layout" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
