[package]
name = "mtrm-keymap"
version = "0.3.7"
edition = "2024"

[dependencies]
mtrm-config = { path = "../config" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
