[package]
name = "mtrm-process"
version = "0.3.5"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
portable-pty = "0.9"
thiserror = "2"
nix = { version = "0.28", features = ["signal", "process", "term"] }

[dev-dependencies]
tempfile = "3"
