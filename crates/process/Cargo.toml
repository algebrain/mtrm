[package]
name = "mtrm-process"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
portable-pty = "0.9"
thiserror = "2"
nix = { version = "0.29", features = ["signal", "process"] }

[dev-dependencies]
tempfile = "3"
